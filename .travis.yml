language: php

php:
  - 5.6

branches:
  only:
    - service/travis

addons:
  apt:
    packages:
    - php5-dev

before_install:
  - wget https://github.com/phpredis/phpredis/archive/develop.zip && unzip develop.zip && cd phpredis-develop
  - phpize
  - ./configure
  - make
  - sudo mkdir -p /etc/php5/cli/conf.d/
  - sudo echo 'extension='`pwd`'/modules/redis.so' > /etc/php5/cli/conf.d/redis.ini

install:
  - composer install --no-dev

services:
  - redis-server

script: phpunit --configuration phpunit.xml.dist --coverage-text